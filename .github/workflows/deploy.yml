name: Manual Deployment Helper

on:
  workflow_dispatch:  # Manual triggering only
  push:
    branches: [main]

jobs:
  check-deployment:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Validate project structure
      run: |
        echo "✅ Project structure is valid"
        echo "📁 Files in repository:"
        ls -la
        echo "🌐 Your website: https://hamzamir-resume-app-gzgwc0d9f2csexgt.eastasia-01.azurewebsites.net"
        echo "💡 Note: Automatic deployment is disabled to prevent errors"
        echo "📋 To deploy manually, use FTP client or Azure Portal"